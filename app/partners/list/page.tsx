import PartnersList from "./partners-list";

// The running partner list. Lives here rather than in the Flow app so Andrew
// has one place for partners alongside the partner calls and dashboard.
export default function PartnersListPage() {
  return <PartnersList />;
}
