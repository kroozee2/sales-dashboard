import CallsWorkspace from "./calls-workspace";

// Sales: leads and triage. Client and partner calls live on their own calendars.
export default function CallsPage() {
  return <CallsWorkspace lane="sales" />;
}
