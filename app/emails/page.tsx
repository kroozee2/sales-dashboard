import EmailsWorkspace from "@/components/emails-workspace";

// Email performance and planning. GoHighLevel sends; this reports on it and
// holds the plan around it.
export default function EmailsPage() {
  return <EmailsWorkspace />;
}
