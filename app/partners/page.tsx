import CallsWorkspace from "../calls/calls-workspace";

// Partners: connection, partnership and JV calls, on the same calendar the
// sales team already knows.
export default function PartnersPage() {
  return <CallsWorkspace lane="partner" />;
}
